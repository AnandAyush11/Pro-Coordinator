import 'package:flutter/material.dart';

class FacultyDashboard extends StatelessWidget {
  const FacultyDashboard({Key? key}) : super(key: key);
  static const routeName = '/FacultyDashboard';
  @override
  Widget build(BuildContext context) {
    return Container();
  }
}
